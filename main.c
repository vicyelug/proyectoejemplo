int main (void)
{
    printf("Hola Mundo");
   // comandos varios:
  //  git clone <repo> (clonar repositorio) git clone git@github.com:vicyelug/proyectoejemplo.git 
  // git fetch (descargar la informacion) // descarga, pero no mezcla
  // git pull origin (descargada la informacion) mover al ultimo commit
  // git commit -am "mensaje" (actualizar) git commit -am "doc 1"
  // git push origin main (empujar cambio) git push -u origin main
  // git status 
  // git checkout main (ir a)
  // git branch Dev1 (crear rama) 
  // git checkout -B Dev2 (crear rama y guardar)

}